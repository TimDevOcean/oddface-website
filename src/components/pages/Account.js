import React from 'react'


export const Account = (props) => {
  return (
    <div className='app-container'>
        <main>
          {props.component}
        </main>
    </div>
  )
}

export default Account;
